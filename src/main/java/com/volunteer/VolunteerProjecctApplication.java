package com.volunteer;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication	
public class VolunteerProjecctApplication {

	public static void main(String[] args) {
		SpringApplication.run(VolunteerProjecctApplication.class, args);
	}
	// next i have to create certificate of the volunteer for their completed their event
}
	// after comit rolback happended 
// set ui expression data to the ui page only